<h1>Комментарии(й):</h1>

<?php if ($paginator->count() != 0): ?>
    <div class="posts-list-4-cols">
        <? foreach ($paginator as $comment): ?>
            <?
            $params = [];
            $params['book'] = $comment->getBook()->getAlias();
            $params['s'] = $comment->getBook()->getZhanr()[0]->getParent()
                ->getAlias();
            $params['alias_menu'] = $comment->getBook()->getZhanr(
            )[0]->getAlias();
            $route_ = 'home/genre/one/book';
            ?>
            <a href="<?= $this->url(
                $route_,
                $params
            ) ?>"><?= $comment->getBook()->getName(); ?>
            </a>
        <? endforeach; ?>
    </div>

    <div class="text-center">
        <?

        echo $this->paginationControl(
            $paginator,
            'Sliding',
            'pagination',
            array(
                'route'  => 'home/cabinet/comments',
                'action' => 'comments',
            )
        ); ?>
    </div>
<?else:?>
    Нет ни одного комментария.
<? endif; ?>


